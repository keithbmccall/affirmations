import { StyleType } from './types';

export const spacingValues: Record<string, number> = {
  standard: 10,
};

export const positionValues: StyleType = {
  absolute: {
    position: 'absolute',
  },
  relative: {
    position: 'relative',
  },
};
