import { LensCameraRoll } from '../../../lib/features/Lens/LensCameraRoll';

export default function CameraRollScreen() {
  console.log('camera roll screen rerenders?');
  return <LensCameraRoll />;
}
